# Copier en .env pour un usage local (.env est ignoré par git).
# Rien d'obligatoire : le domaine se configure dans l'UI Coolify,
# champ "Domains", pas ici.

# Dossier de la base SQLite (défaut : /data dans le conteneur)
# NOTASK_DATA_DIR=./data
