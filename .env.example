# Copier en .env pour un usage local (.env est ignoré par git).
# En production, cette valeur se saisit dans l'UI Coolify :
#   Ressource > Environment Variables > APP_DOMAIN
# Nom d'hôte seul, sans https:// ni port.
APP_DOMAIN=notask.exemple.tld
